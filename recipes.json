{
  "build": {
    "cmd": "wasm-pack build client --dev"
  },
  "run": {
    "cmd": "npm run start --open",
    "cwd": "www",
    "env": {
      "NODE_OPTIONS": "--openssl-legacy-provider",
      "RUST_LOG": "info"
    }
  }
}

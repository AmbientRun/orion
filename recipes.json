{
  "build": {
    "cmd": "make build",
    "components": {
      "qf": {
        "compiler": "cargo"
      }
    }
  },
  "run": {
    "cmd": "npm run start --open",
    "cwd": "www",
    "env": {
      "RUST_LOG": "info"
    }
  }
}

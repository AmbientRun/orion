{
  "build": {
    "cmd": "make build",
    "components": {
      "qf": {
        "compiler": "cargo"
      }
    }
  },
  "run": {
    "cmd": "npm run start --open",
    "cwd": "www",
    "env": {
      "NODE_OPTIONS": "--openssl-legacy-provider",
      "RUST_LOG": "info"
    }
  }
}
